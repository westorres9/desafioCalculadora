export default function Calculate(){}

    Calculate.prototype.plus = function(number1, number2) {
        return number1 + number2;
    }

    Calculate.prototype.times = function(number1, number2) {
        return number1 * number2;
    }
